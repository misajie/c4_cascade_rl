numpy>=1.24
pandas>=2.0
pyarrow>=14.0
pyyaml>=6.0
scipy>=1.10
scikit-learn>=1.3
tqdm>=4.65
click>=8.1
pytest>=7.4
pytest-cov>=4.1
# optional: do NOT overwrite cluster torch — install only if missing:
# torch>=2.0
# optional: rdkit for scaffold clustering (skip tests if missing)
# rdkit-pypi
# optional PEFT/transformers for LLM LoRA (dry-run without GPU)
# transformers>=4.36
# peft>=0.7
# datasets>=2.14
